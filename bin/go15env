#!/bin/bash

export PATH=/usr/local/go1.5/bin:$PATH
export GOROOT=/usr/local/go1.5
export GOPATH=~/gprojects15/$(basename $1)
#export GO15VENDOREXPERIMENT=1
cd ~/gprojects15/$(basename $1)/src/github.com/$1
PS1="($(basename $(pwd)))$PS1"
